<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	<appSettings />
	<connectionStrings>
		<add name="WMSSyncService.Properties.Settings.ORADB" connectionString="Data Source=ATLANTIS;Persist Security Info=True;User ID=ATLANTISERP;Password=atlantis000;Unicode=True" providerName="System.Data.OracleClient" />
	</connectionStrings>
	<system.web>
		<!-- 
            Set compilation debug="true" to insert debugging 
            symbols into the compiled page. Because this 
            affects performance, set this value to true only 
            during development.
        -->
		<compilation debug="true">
		</compilation>
		<!--
            The <authentication> section enables configuration 
            of the security authentication mode used by 
            ASP.NET to identify an incoming user. 
        -->
		<authentication mode="Windows" />
	</system.web>
	<system.codedom>
	</system.codedom>
	<!-- 
        The system.webServer section is required for running ASP.NET AJAX under Internet
        Information Services 7.0.  It is not necessary for previous version of IIS.
    -->
	<system.webServer>
        <defaultDocument>
            <files>
                <add value="WMSSyncService.asmx" />
            </files>
        </defaultDocument>
        <directoryBrowse enabled="true" />
	</system.webServer>
<startup><supportedRuntime version="v2.0.50727" /></startup></configuration>
